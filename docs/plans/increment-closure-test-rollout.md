# Increment closure test rollout

1. Add the explicit settings-file override and tests without changing production precedence.
2. Attach refno/name accessibility identity to actual model tree rows and update automation matching.
3. Track data-task, model-task, pending and view-refresh barriers before declaring an update settled.
4. Run rename/add/delete tests for equipment and piping models against the isolated profile.
