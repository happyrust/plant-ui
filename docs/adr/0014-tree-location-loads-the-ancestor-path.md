# 树定位只补齐目标的祖先路径

模型树继续按 SITE 根层懒加载；当树定位的目标尚未物化时，客户端异步查询目标的祖先链，只展开当前 MDB 内的祖先并加载这些祖先缺失的直接子层，等目标真正进入可见行后再滚动。这样避免为一次定位预载整棵模型树，也避免一次性的滚动请求在异步子层返回前被消费。

`=参考号` 与 `/名称` 共用这条路径。树定位立即选中目标并加载属性，但不切换 MDB、不生成或显示三维模型，也不移动相机；连续定位只完成最后一次，查询失败或用户手动折叠目标路径都会结束待滚动。
