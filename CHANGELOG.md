# 变更记录

写给用的人看：每一条说的是**界面上会不一样的地方**，不是改了哪个函数。
实现层的决策去 `docs/adr/`，施工顺序去 `docs/plans/`。

版本口径按 `Cargo.toml` 的 `workspace.package.version`。

## 未发布

### 设置

- **模型树、属性、搜索和三维实例从哪儿读，成了一项设置。** 设置窗「服务」一节、模型服务
  地址底下多了一格「供数模式」下拉，两项：**服务供数**（出厂默认，经模型服务的接口读，
  底下是 e3d-io 与 e3d-model）与**库供数**（直连 SurrealDB，v0.1.9 及之前的读法；留给
  已落盘的 rocksdb 部署、老版本模型服务与对拍）。整个接入点只认一种，不按面混；补齐 /
  更新 / 队列这些命令面与它无关，照旧走模型服务。状态栏那枚数据库芯片点开的接入点面板
  多一行「供数」，报的是此刻真正生效的那一面。
- **换了当场生效，不重启。** 保存之后三维立刻清空、树与属性归零，用新的一面重新连上，
  然后按换面前的样子把已加载的模型重装回来——显示 / 隐藏原样恢复，相机不动；命令行视图
  与日志各一句「供数模式：服务供数 → 库供数；已清场，重装 N 个已加载模型 / M 个范围目标」。
  新的一面连不上就停在未连接态，红字带「重试」，下拉可以再改回去。
- **开发期可用环境变量 `PLANT_READ_FACE=service|store` 压过设置。** 在场时下拉灰掉，悬停
  与右下一行黄字写明「由 PLANT_READ_FACE=… 压过」；认不出的值启动时出声一次、按设置走。
  v0.1.9 那个只管树的 `PLANT_TREE_DATA_MODE` 不再认。老 `settings.ron` 没有这一格就是
  服务供数。
- **库供数对着现在的模型服务写出来的库，树不再是空的。** 此前切到库供数或以
  `PLANT_READ_FACE=store` 启动，会连上却报「根层 0 个 SITE」、树面板「当前库下没有
  SITE」，三维实例却照常——`MDB` 里设计库的类型标记在新写的库里是文字 `"1"`、老库里是
  数字 1，解设计库号那条查询只认数字，一个库都没解出来。现在两种写法都认。同时修好
  `PLANT_READ_FACE=store` 启动即报「数据源连接失败：Connection uninitialised」：工程标识
  与根层两条并发查询各自等连接落地，不再抢在前头。
- **库供数拿不到的几格，现在会说出来，不再安静空着。** 服务供数有而它没有的东西有四处：
  队列面板里库行那格「模型来源」整格不画（那说的是**服务端**从内存还是 rocksdb 取模型，
  而库供数的三维实例根本不经服务端，画哪个都是替它说话）；服务端翻面的通告照记，但明说
  「本客户端库供数，不受影响」；点开元件库元素，属性面板给一句「元件库元素不入模型本体库；
  切到服务供数可看」，而不是一张只剩参考号的空表——设计库元素查空则说「库里没有这个元素的
  属性（未同步）」，模型服务不在场判不出是哪一种时只说「属性为空」，不猜；搜索下拉的空结果
  与子串那一段的段首标出「（只覆盖已入库元素）」。
- **模型服务不在场时，库供数不再说得像整个客户端断了。** 队列面板此前一律写「读不到任务
  队列」；库供数下它现在写「模型服务离线」，并补一句「库供数下树 / 属性 / 已生成模型照常；
  补齐、更新、房间要等它回来」。启动本身不等模型服务——工程标识与根层都直连库。
- **两种供数模式可以对拍了。** `plant-ui-app --read-face-parity`（或
  `scripts\Run-ReadFaceParity.ps1`）不开窗，对着同一个接入点把服务供数与库供数各读一遍：
  SITE 根层与往下几层的成员集合与原序、抽样元素的属性逐字段（相同 / 只差首尾空白 / 只差括号
  与空格 / 不同，再按字段名归堆）、抽样根的三维实例按库分桶，落成一份 Markdown 报告
  （默认 `docs/evidence/<日期>-read-face-parity.md`）。报告只列差异不判对错：服务供数读文件
  最新、库供数读库里的水位，两边时点可以不同，报告头把 `/dbnums` 的两枚会话号摆在前面。
  要模型服务与 SurrealDB 都在场；`--service` 可压过设置里的模型服务地址。

### 搜索

- **右上角那个搜索框能用了。** 此前它是一张画：点不动，也没接任何东西。现在打名字
  或参考号，下拉里列出候选（类型 + 全名 + 参考号），回车或点一下就定位到模型树上
  ——与命令行的 `/名称`、`=参考号` 走的是同一条路。Ctrl K 直接聚到框里，上下键挑，
  Esc 收起。
- **名字打一半就行，开头那个 `/` 也可以省。** 跟着输入走的是前缀匹配，落在库里的
  名称索引上，八百多万行的库里也是一眨眼。
- **前缀搜空了，再按回车逐条搜名字中间那一段。** 这一步没有索引可用，要把当前 MDB
  的设计库一行行读过去，几秒起步；所以它只跟回车走，不跟按键走。下拉里会先把这句话
  连同搜索范围（几个设计库）摆出来，按不按由你定。
- **命中不在当前模型树里的，先说清楚。** 这类元素（多半在元件库里）标「树外」：
  选得中、属性看得到，但树上没有它那一行，也不会替你切 MDB。
- **修好了下拉列表塌成一条线、文字漏到界面上的毛病。** 打字快过结果回来时，下拉会有
  一帧空着，宽度就此归零且再也长不回来：参考号贴到搜索框左边、名字被挤得只剩几个字、
  整片文字盖在三维视图上。现在下拉自己撑住宽度，等结果的那一帧也会写明「搜索中…」。
- **服务供数下不再说「按中间片段搜索仅桌面端提供」。** 那一面搜的是模型服务的名称索引，
  它本来就按中间片段匹配；这句话是从只有本地子串索引的年代留下来的，说反了。它现在只在
  库供数下出现——那一面的子串确实来自本地索引。

### 三维与交互

- 恢复右键长按左右拖动的 CAD 旋转手感；水平拖动不再与场景旋转方向相反。
- 修复带负实体的构件在平表缓存尚未回填时退回正体网格的问题；布尔成品现在始终以
  单个单位变换实例显示，不再把原语缩放重复施加。`1RX-RM13-DOME` 在右视图中恢复为半球。
- 直管段连接到已删除的管件时，不再继续显示成一根正常实体管；现在按 E3D 的错误
  TUBI 语义，以固定真实节距的三维虚线中心线显示。正常直管仍保持完整实体外观。
- **正在初始化的库现在也能装模型。** 模型服务以 rocksdb 形态运行、某个库的初始化还没
  收口时，此前点眼睛要么原地等、要么报错；现在服务端直接从库文件把它算出来供给界面，
  日志在计数后面说明「其中 dbN 由 API 从内存供数（初始化收口后自动改读 rocksdb，不必重载）」。
  已初始化的库照旧以 rocksdb 为准，日志不多说。
- **同一个库的模型不会从两个来源拼出一个视口。** 初始化刚好在一次装载的两次请求之间收口，
  这一趟会以「混了两个取数源」失败并写明重查即可，而不是把两版几何拼成一个内部不一致的场景。
  跨库各自的来源不同属正常，不算混源。

### 取回工作

- 点下去那一刻三维立即清空，随后看着模型按库里此刻的样子重新长出来——旧几何不再在
  冷查询期间冒充新数据。
- 重查范围从「全部 SITE 根」收窄为「取回前场景里存在的那批模型」：显示中与已隐藏的
  逐个重查重建；新增元素只进树、三维保持未加载，库里删掉的自然消失。
- 显示 / 隐藏在取回后原样恢复，相机也不再被拉去看全场。
- 后台还有更新任务时，取回的结果不再被整包丢弃，查回来一律上屏；查询失败时场景保持
  清空并给出红字，队列空闲后自动补载一次。
- **重装之前先把模型追到库文件最新。** 模型服务以 direct 形态运行时，设计人员在 E3D 里
  存过盘、没跑手动更新，此前取回工作重装出来的三维还是上一次生成的几何。现在清场之后
  会先对取回前点过眼睛的那些范围逐个补齐模型（与点一次眼睛走的是同一条路，没改过的
  部分直接命中缓存），再重查重装；日志写明「核对 N 个范围 → 重算 g 个生成根、命中 c 个」。
  某个范围补齐失败不影响其余部分重装，但会在日志里点名并给出路（比如服务端还在后台
  生成，稍后再点一次）。这一步只动模型，不推进数据水位、不发起手动增量更新。
- **菜单里那行「设计库还有 N 个会话未应用」改说「N 次保存未应用」，并单独数出「M 个库
  需初始化」。** 此前它直读 gen-model 的水位表——零解析部署里那张表是空的，于是从未导入
  的库既不算保存也不出声；现在从任务队列每拍轮询的库状态算，需初始化的库单独一句
  （它没有「待应用的保存」可数，但确实会跑）。排除、阻断、够不着的库不计入。

### 模型增量更新

- 数据批次跑完后模型树立刻换新，不再等整场三维重载先跑完那十几秒的冷查询。
- 这种自动刷新只换树：三维停在旧几何上，日志会把这句说出来，要换新点菜单「取回工作」。
- **模型服务以 direct 形态运行、没起数据批次 worker 时，执行入口会灰掉并说明原因。**
  此前「开始更新」「立刻扫一遍」「立刻重试」照常可点，请求入队之后没人出队，队列里
  那一行永远「排队中」，界面上看不出任何异样。现在任务队列顶部有一条横幅、向导脚注
  与灰掉的按钮都写明「预览可用，执行不可用」；预览、暂停 / 恢复队列不受影响。
  服务端起了 worker 之后，同一界面不必重启就自动恢复可用。老版本服务端不报形态，
  行为与此前一致。
- **库行上多了「模型来源」。** 类型旁一个字形：数据库（rocksdb 为准，重启后仍在）或内存
  （由 API 现算供数、活不过服务端重启）；悬停与行内明细里是整句，内存那一种会说具体
  ——「初始化中」「未初始化」「读透」。说明列空着时整句就摆在那里。
- **初始化收口那一刻，这一格自己翻成「数据库」，三维不重载。** 已经显示的模型与刚落库的
  是同一版文件算出来的，下一次范围重载自然读到 rocksdb；日志说一句。相反方向（库被清空
  待重建）同样只翻这一格。老版本服务端不报来源，这一格整个不画。

## 0.1.8 — 2026-08-11

### 三维与交互

- 修复三维视图右键拖动时左右旋转方向相反的问题。
- 保存与历史记录相关界面统一使用保存时间描述，不再显示容易混淆的会话编号。

### 数据连接

- 修复重新连接数据源时旧查询缓存未清理、可能继续显示旧项目数据的问题。

## 0.1.6 — 2026-08-10

### 三维与交互

- 房间面板支持自动 X-Ray，查看房间内部时不再被外层面板遮挡。
- 修复停靠面板缩放时三维视口闪烁的问题。
- 修复发布构建误用仅调试版 egui 样式选项的问题。
- 命令行现在支持模型查询。

### 配置与发布

- 示例配置的 MDB 与模型服务保持一致，减少更新时的身份不匹配。
- GitHub Release 同时发布 Windows x64 与可独立部署的 WASM Web 压缩包。

## 0.1.5 — 2026-08-08

### 模型增量更新

- **失败的交付单元现在说得出为什么失败，也有出路。** 已达重试上限的单元不再顶着
  「后台自动重试」——那句话对它们是字面错误，自动路径永不再碰它们，模型会一直停
  在旧几何。现在它们标「已放弃」，行内给一枚「立刻重试」；每一行下面补出上次的
  失败原因，这份原因跨得过重启（实时通道那份只活在进程内存里，重连即失）。
- **状态栏与队列面板会报「N 个单元已放弃重试」。** 它不并进「队列 N」——那一格数
  的是还会被干掉的活。队列空成 0 的时候这个数更要在，那正是最容易以为都干完了
  的一刻。
- **更新预览把「会合并的」与「已放弃的」拆成两张卡。** 之前两者混在「N 个待重试
  单元将合并」里，而已放弃的那些这一次一个都不会跑。一个只剩已放弃单元的项目里，
  「开始更新」不再永远亮着。
- **确认执行后的回执把扫描到的库全部交代清楚。** 之前只要有任意一条入队，「已是
  最新」与「已被排队中的批次覆盖」两个数就整个消失，人看到「扫描 29 个库；已入队
  2 个数据批次」而剩下 27 个去哪了说不出来。五个去向现在各报各的，加不到总数时
  还会说「另有 N 个库回执没交代去向」。

### 设置

- **网格目录成了一项设置，改完当场生效。** 三维网格此前只能从资产根底下那个固定的
  `meshes` 里取，换一个位置得改环境变量再重启整个应用；现在设置窗「资产」一节里既
  能直接填，也能用系统对话框挑，保存之后先前加载失败的网格立刻重来一遍——已经画出来
  的那些不动，网格文件名是内容哈希，换个目录取到的是同一份。留空则沿用原来的默认
  位置，现有部署一切照旧。
- **选错一层当场看得出来。** 指到资产根而不是它底下的 `meshes` 是最常犯的错，路径本身
  又确实存在；现在这种情况下面会给一句黄字，不用等三维全红才发现。
- **设置终于记得住了。** 主题、界面密度、两个服务地址连同网格目录一起存进可执行文件
  旁的 `config/settings.ron`，重启不再回到默认值。**顺带一个要留意的变化**：这些项
  从此按「设置项 > 环境变量 > 出厂默认」排序，启动脚本里设的 `PLANT_MODEL_API_URL`
  之类会被上一次在界面里存下的值顶掉。
- 浏览器端「网格目录」那一行是灰的：那一侧的网格与字体贴图一样由站点供给，不读本地目录。

### 房间

- **房间自己的模型能一键看见了。** 右键「查看所属房间 ▸」子菜单底部新增
  「显示房间模型（房间号）」，作用于主归属房间；「房间」页签的操作行补了一枚
  同名芯片，摆在「缩放到房间」前面。点它把这间房的墙面板几何加载进三维，落地
  后取景过去。此前点房间行走的是隔离加取景，两者都只对已经加载的模型生效，而
  房间面板多半从没被加载过——结果往往是别的模型全被隐藏、房间本身仍然什么都
  看不见。

### 任务队列

- **「本期不执行」从两档改成三档：阻断 / 够不着 / 排除。** 「够不着」是 MDB 声明
  了这个库、当前项目目录里却没有它的文件，与「排除」（当前 MDB 没声明它）意思正好
  相反，之前只能借用后者那句话。这些库此前在面板上整个不出现，「MDB 说 29 个，
  这里怎么只有 20 个」没有出处。
- **标题按三个理由分开报数**（如「1 个阻断 · 1 个够不着 · 2 个不在范围内」），
  两百多行滚在一个小框里不再只说一句「本期不执行」。
- **浏览器端不再被讲成「断线」。** web 构建本来就不建实时连接、进度走轮询，之前
  却让每一条运行中的行都挂上「断线期间没收到事件 · 明细缺 N 条」——那个 N 恰好
  等于服务端发过的全部条数。现在只说一句「这个构建不收逐单元明细，进度按轮询走」，
  不报条数、不给重连。

### 更早（0.1.4 发版包之后）

- 更新面板显示纯位姿变更目标：只刷新变换、不重建网格。
- 桌面端中文输入法可用。
- 三维：直管段全部渲染；偏心缩放锚点不再漂移；首个由 eye 加载的模型自动取景。
- 模型树 eye 与三维实际渲染结果同步，容器行的状态向下继承。
- 模型加载不再占住交互队列，进度并进状态栏。
- 顶栏显示项目名与项目代号；设置窗布局与 SITE 模型查询修正；命令菜单打磨。
- 属性面板切换不再闪断；可选中的属性文本修正。
- 发版包支持外部 legacy 资源根目录。

## 0.1.4 — 2026-07-29

首个独立发版包，含 standalone web 构建。
