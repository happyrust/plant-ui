# plant-ui

布置平台的桌面外壳。模型树、属性、命令行、三维视口四个视图，加上模型增量更新等任务窗。
数据来自 SurrealDB（模型本体）与 gen-model 服务（增量更新），本文件只统一说法，不记实现。

## Language

### 模型增量更新

**手动增量更新**:
由人在界面上发起的一次「把设计库的新会话应用到模型」的操作，分预览、确认、执行三步。
_Avoid_: 同步、刷新、导入

**自动同步**:
服务端自行追新的模式，开启时手动增量更新被拒绝。两者互斥，不会同时生效。
_Avoid_: 实时同步、自动更新（作动词时容易和「手动增量更新」混）

**数据批次**:
一个 dbnum 在一个会话号区间上的一次数据应用。执行的最小单位，按 dbnum 串行。
_Avoid_: 库更新、批、数据包

**交付单元**:
模型生成的最小根，由变化元素向上解析得到，`noun + root_refno` 唯一标识。
_Avoid_: 生成单元、模型块、构件、ZONE 单元

**水位**:
某个 dbnum 已经应用到的会话号。只前进不后退，数据批次成功才推进。
_Avoid_: 进度、位点、版本号

**并入会话**:
预览扫描之后、执行真正开始之前新产生的会话。执行时自动并进同一个数据批次，结果摘要必须逐条列出。
_Avoid_: 新会话、追加会话、补扫

**ZONE 分桶**:
把受影响的交付单元按所属 ZONE 归类的展示手段。既不是数据边界也不是生成根，不影响执行范围。
_Avoid_: ZONE 边界、ZONE 单元、按 ZONE 更新

**归属未知**:
解析不到所属 ZONE 的那一桶变化。只影响怎么摆给人看，不阻断任何东西。
_Avoid_: 未分类、异常 ZONE

**no_generation**:
向上找不到任何合法交付单元的变化计数。这些变化跳过模型生成并告警，不会退化成一个 ZONE 级单元。
_Avoid_: 跳过项、无根变化

**待重试单元**:
上一次生成失败、留待下次一并处理的交付单元，带累计尝试次数。数据已经写入，只欠模型。
_Avoid_: 失败任务、重试队列

**文件异常**:
设计库文件本身出的问题，共五种：会话号回退、路径迁移、类型变化、同号重复、文件缺失。
_Avoid_: 错误、告警

**阻断**:
因文件异常而不执行某个 dbnum。它在执行范围之内，只是这次不跑。
_Avoid_: 跳过、失败

**排除**:
非 DESI 的库压根不进入本期执行范围，与阻断是两回事。
_Avoid_: 跳过、忽略

**运行**:
一次手动增量更新的执行实例，有 task_id 和 succeeded / partial / failed 三种终态。
_Avoid_: 任务、作业、job

**部分完成**:
一次运行里既有成功也有失败的终态（partial）。数据批次已推进的水位不因模型生成失败而回退。
_Avoid_: 半成功、部分失败、未完成
