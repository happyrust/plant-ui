# plant-ui

布置平台的桌面外壳。模型树、属性、命令行、三维视口、任务队列五个视图，加上模型增量更新、三维数据提资等任务窗。
数据来自 SurrealDB（模型本体）与 gen-model 服务（增量更新），提资经 rs-server 交数据中心；本文件只统一说法，不记实现。

## Language

### 项目与连接

**项目接入点**:
一个项目对应的一整组服务地址与身份：模型本体库（SurrealDB 的 ns/db）、模型服务、数据中心，
外加项目名、项目号与 MDB。切项目就是换一个接入点。
_Avoid_: 一组端点、项目配置、连接、数据源

### 模型导航

**树定位**:
选中目标元素，展开模型树中的祖先，并把目标行滚动到可视区域。它不生成或显示三维模型，也不移动三维相机。
_Avoid_: 跳转、定位模型、聚焦

**模型聚焦**:
把三维相机移动到目标模型的包围范围。它不负责展开或滚动模型树。
_Avoid_: 树定位、跳转

**树外元素**:
存在于模型本体中，但不属于当前 MDB 模型树根范围的元素。树定位可以选中并显示它的属性，但不切换 MDB。
_Avoid_: 不存在、未加载、未展开

### 模型可见性

**实际可见性**:
View3d 回执给宿主的模型渲染结果，包含实体可见方向与 mesh 成功 / 失败计数。模型树 eye
只读这份结果；加载期间的显示 / 隐藏方向另行暂存，不提前改变 eye。
_Avoid_: 可见性台账、显示缓存、visibility map

**未加载**:
三维里没有可渲染 mesh 的元素：可能从未显示，也可能查询为空、查询失败或所有 mesh
都加载失败。它既不是显示也不是隐藏——画面里压根没有可见内容。
_Avoid_: 隐藏、不可见、未展开

**已隐藏**:
被显示指令提到过、方向是隐藏的元素。实体存在，只是这一刻不画。与「未加载」的分别是有没有人动过它。
_Avoid_: 不可见、关闭

**容器行**:
模型树上自己不带几何的行（SITE / ZONE / PIPE 等）。它的 eye 聚合该行已经解析并缓存的
实际模型范围；尚未单独解析范围的后代继承最近祖先的实际状态，直到自己查询出结果、
明确查空或查询失败。后代有显示有隐藏时为半可见。
_Avoid_: 父节点、分组行、非叶子

**几何行**:
在三维里有 mesh 实体的行。它的 eye 直接读取 View3d 回执；可见但只有部分 mesh 成功时为半可见。
_Avoid_: 叶子、实例、零件

### 模型增量更新

**保存**:
设计人员在 E3D / PDMS 里存一次盘，设计库就多一条会话记录（内部序号 sesno 单调递增）和它的写入时刻。
界面语言一律说「保存」（`N 次保存`、`保存于 …`、时刻用 E3D 写入时刻）；「会话 / sesno」只存在于
代码、契约与日志（ADR-0019）。命令行视图的「命令会话」是另一概念，不受此约束。
_Avoid_: 会话、session、sesno（界面上）

**本期执行范围**:
当前 MDB 在 CURD 里声明为 DESI 的那些库。增量更新只碰这些库——CATA 之类不碰，MDB 之外的设计库也不碰。
取的是 MDB **声明**的名单，不是「已经解析出 SITE 的库」：声明了却从没导入过的库该出现在范围里等人确认。
SYS meta（SYST / DICT / GLB / GLOB）不受这道门管，名单本身就存在它们里面。
_Avoid_: 扫描范围、项目范围、白名单

**手动增量更新**:
由人在界面上发起的一次「把设计库里新的保存应用到模型」的操作，分预览与确认两步，确认之后进任务队列。
_Avoid_: 同步、刷新、导入

**自动同步**:
服务端自行追新的模式。它与手动增量更新产出同一种数据批次、走同一条任务队列，两者不再互斥。
_Avoid_: 实时同步、自动更新（作动词时容易和「手动增量更新」混）

**取回工作**:
把库里此刻的样子取到界面上来。模型树与已加载的三维模型都算在内，**不应用任何数据**。
它不发起手动增量更新，两种同步模式下行为一致。
_Avoid_: 同步、更新、重连

**数据批次**:
一个 dbnum 在一个连续保存区间上的一次数据应用（内部以会话号区间界定）。执行的最小单位，按 dbnum 串行。
界面上的窗口时间对取区间自身两端：第一条待应用保存的写入时刻 → 最后一条的写入时刻（ADR-0019）。
_Avoid_: 库更新、批、数据包

**交付单元**:
模型生成的最小根，由变化元素向上解析得到，`noun + root_refno` 唯一标识。
_Avoid_: 生成单元、模型块、构件、ZONE 单元

**水位**:
某个 dbnum 已经应用到的位置（内部是会话号）。只前进不后退，数据批次成功才推进。
界面以该保存的写入时刻指代水位位置（`水位推进至 08-07 14:33`）；回退检测与区间起点仍用内部序号（ADR-0019）。
_Avoid_: 进度、位点、版本号

**并入保存**:
数据批次还在排队、尚未冻结时新产生的保存。执行时自动并进同一个数据批次，结果摘要必须逐条列出，
逐条带写入时刻（同一分钟内重复时该条补秒，ADR-0019）。
_Avoid_: 并入会话、新会话、追加会话、补扫

**no_generation**:
向上找不到任何合法交付单元的变化计数。这些变化跳过模型生成并告警，不会退化成一个 ZONE 级单元。
_Avoid_: 跳过项、无根变化

**待重试单元**:
上一次生成失败、留待下次一并处理的交付单元，带累计尝试次数。数据已经写入，只欠模型。
_Avoid_: 失败任务、重试队列

**文件异常**:
设计库文件本身出的问题，共五种：文件回退（最新保存早于已应用）、路径迁移、类型变化、同号重复、文件缺失。
_Avoid_: 错误、告警

**阻断**:
因文件异常而不执行某个 dbnum。它在执行范围之内，只是这次不跑。
_Avoid_: 跳过、失败

**排除**:
压根不进入本期执行范围的库，与阻断是两回事——阻断的库在范围之内，只是这次不跑。
排除有两个理由，界面上必须分开数：非 DESI，以及不在当前 MDB 声明的名单里。后者连行都没有。
_Avoid_: 跳过、忽略

**够不着**:
当前 MDB 声明了这个库，但当前项目目录里没有它的文件——多半在别的项目里（`/ALL` 声明的 29 个 DESI
有 9 个是 AvevaCatalogue 的模板与标准库），而扫描只走当前项目目录。它与「文件缺失」的分别是**有没有
人登记过**：缺失是登记过的文件不见了，得阻断；够不着的从没登记过，本期只是碰不到它，不阻断也不执行。
_Avoid_: 缺失、丢失、不存在

**任务队列**:
所有等着被应用的数据批次排成的一条线。手动与自动两条来源合流于此，先进先出。
_Avoid_: 任务列表、作业队列、pending 表

**冻结**:
一个数据批次开始执行的那一刻，它的保存区间就定死了。此后新存的保存不再并入它，另起一条排队行。
_Avoid_: 锁定、快照、封版

**部分完成**:
一个数据批次里既有成功也有失败的终态（partial）。已推进的水位不因模型生成失败而回退。
_Avoid_: 半成功、部分失败、未完成

**终态**:
数据批次跑完后的三种结局之一：已完成 / 部分完成 / 失败。失败指批次本身没落库、水位未动；整次更新还有第四种「已是最新」——压根无事可做。
_Avoid_: 完成状态、结束态

**需初始化**:
从未导入过、还没有权威水位的设计库。契约不为它解保存区间、变化数全是零，但它确实会跑并产生数据批次——绝不能显示成「无变化」。
_Avoid_: 空库、新库、无变化

**入队回执**:
确认执行后服务端返回的「已入队，排在第 N 位」凭据。执行请求一律入队，不存在「已有任务在跑」的拒绝。
_Avoid_: 执行结果、任务凭证

**队列重建**:
服务重启后按水位重新扫出来的队列。工作不丢，但排队时长从重启起算，重启前排了多久无从得知——界面必须把这两句都说出来。
_Avoid_: 队列恢复、断点续传

**行内明细**:
队列行点开后的展开区，装两阶段进度与终态摘要。默认折叠，只有运行中的行默认展开。
_Avoid_: 详情面板、下钻

**房间泳道**:
任务队列里与设计库行平级的一条跨库横道，装房间归属重算的进度。房间任务不属于任何单一设计库，不挂在库行下。
_Avoid_: 房间行、房间任务组

**饥饿态**:
队列长期不空、房间归属重算一直排不上的状态。这段窗口里材料表读到的房间号可能是旧的，且没有任何环节会报错——界面是唯一的提示面。
_Avoid_: 超时、阻塞

### 三维数据提资

**提资**:
把人工挑选元素的三维数据交给数据中心的一次操作，按专业（工艺 / 电气 / 仪控 / 通风）成包提交，经 rs-server 转数据中心。它不改动模型本体，也不参与增量更新。
_Avoid_: 数据发布、数据接口、预发布、全量发布

**管道房间号查询**:
按元素名称向数据中心问「这根管道落在哪个房间」的查询，不交出任何数据。它不是提资，只是暂时寄居在提资任务窗里。
_Avoid_: 房间数据发布、房间提资

### 同步显示

**模型变更通告**:
服务端发来的「这个参考号的模型产物已经变了」的告知，分重生成、仅位姿、已删除三种。它陈述事实，不下命令——收到之后画不画由本侧按可见性台账决定。
_Avoid_: 局部刷新、增量推送、刷新事件

**重画**:
把已经在场景里的某个模型按库里此刻的样子重新画一遍。它不改变可见性台账的方向，也不移动相机，也不改变当前选中。
_Avoid_: 局部刷新、刷新模型、重新加载

**陈旧标记**:
收到模型变更通告、但这一刻不重画时挂上的标记，意思是「你看到的这份已经不是库里那份」。取消隐藏或人工刷新时消解。它是「不说谎」的兜底：凡是重画不成立的场合都必须挂上，不许装作没事。
_Avoid_: 脏标记、过期、待刷新
