# plant-ui

布置平台的桌面外壳。模型树、属性、命令行、三维视口、任务队列五个视图，加上模型增量更新等任务窗。
数据来自 SurrealDB（模型本体）与 gen-model 服务（增量更新），本文件只统一说法，不记实现。

## Language

### 模型可见性

**可见性台账**:
宿主侧记录每个参考号最后一次收到的显示 / 隐藏指令方向的那份数据。记的是**下过的指令**，不是三维场景里此刻的渲染结果。
_Avoid_: 可见性状态、显示缓存、visibility map

**未加载**:
从来没有被显示指令提到过的元素。它既不是显示也不是隐藏——三维里压根没有它的实体。
_Avoid_: 隐藏、不可见、未展开

**已隐藏**:
被显示指令提到过、方向是隐藏的元素。实体存在，只是这一刻不画。与「未加载」的分别是有没有人动过它。
_Avoid_: 不可见、关闭

**容器行**:
模型树上自己不带几何的行（SITE / ZONE / PIPE 等）。它的可见性只能来自台账，从子树推不出来。
_Avoid_: 父节点、分组行、非叶子

**几何行**:
在三维里有 mesh 实体的行。只有它的可见性能从渲染侧直接读到。
_Avoid_: 叶子、实例、零件

### 模型增量更新

**手动增量更新**:
由人在界面上发起的一次「把设计库的新会话应用到模型」的操作，分预览与确认两步，确认之后进任务队列。
_Avoid_: 同步、刷新、导入

**自动同步**:
服务端自行追新的模式。它与手动增量更新产出同一种数据批次、走同一条任务队列，两者不再互斥。
_Avoid_: 实时同步、自动更新（作动词时容易和「手动增量更新」混）

**取回工作**:
把库里此刻的样子取到界面上来。模型树与已加载的三维模型都算在内，**不应用任何数据**。
它不发起手动增量更新，两种同步模式下行为一致。
_Avoid_: 同步、更新、重连

**数据批次**:
一个 dbnum 在一个会话号区间上的一次数据应用。执行的最小单位，按 dbnum 串行。
_Avoid_: 库更新、批、数据包

**交付单元**:
模型生成的最小根，由变化元素向上解析得到，`noun + root_refno` 唯一标识。
_Avoid_: 生成单元、模型块、构件、ZONE 单元

**水位**:
某个 dbnum 已经应用到的会话号。只前进不后退，数据批次成功才推进。
_Avoid_: 进度、位点、版本号

**并入会话**:
数据批次还在排队、尚未冻结时新产生的会话。执行时自动并进同一个数据批次，结果摘要必须逐条列出。
_Avoid_: 新会话、追加会话、补扫

**no_generation**:
向上找不到任何合法交付单元的变化计数。这些变化跳过模型生成并告警，不会退化成一个 ZONE 级单元。
_Avoid_: 跳过项、无根变化

**待重试单元**:
上一次生成失败、留待下次一并处理的交付单元，带累计尝试次数。数据已经写入，只欠模型。
_Avoid_: 失败任务、重试队列

**文件异常**:
设计库文件本身出的问题，共五种：会话号回退、路径迁移、类型变化、同号重复、文件缺失。
_Avoid_: 错误、告警

**阻断**:
因文件异常而不执行某个 dbnum。它在执行范围之内，只是这次不跑。
_Avoid_: 跳过、失败

**排除**:
非 DESI 的库压根不进入本期执行范围，与阻断是两回事。
_Avoid_: 跳过、忽略

**任务队列**:
所有等着被应用的数据批次排成的一条线。手动与自动两条来源合流于此，先进先出。
_Avoid_: 任务列表、作业队列、pending 表

**冻结**:
一个数据批次开始执行的那一刻，它的会话区间就定死了。此后新存的会话不再并入它，另起一条排队行。
_Avoid_: 锁定、快照、封版

**部分完成**:
一个数据批次里既有成功也有失败的终态（partial）。已推进的水位不因模型生成失败而回退。
_Avoid_: 半成功、部分失败、未完成
